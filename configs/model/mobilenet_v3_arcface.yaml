# === MobileNetV3 + ArcFace Model Configuration ===

model:
  backbone: mobilenet_v3_large   # hoặc mobilenet_v3_small
  pretrained: true
  embedding_dim: 512
  dropout: 0.2

head:
  type: arcface
  s: 64.0
  m: 0.5
  easy_margin: false
